service.identifier = 'palm://org.webosinternals.syshealth';

function service(){};

/*
service.method = function(callback)
{
	var request = new Mojo.Service.Request(service.identifier,
	{
		method: 'method',
		onSuccess: callback,
		onFailure: callback
	});
	return request;
};
*/

// Local Variables:
// tab-width: 4
// End:
